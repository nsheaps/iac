#!/usr/bin/env bash
#MISE description="Run pulumi preview (plan changes without applying)"
#MISE usage="preview [--stack <stack>]"
set -euo pipefail

exec "$(git rev-parse --show-toplevel)/bin/pulumi-wrapper.sh" preview "$@"
